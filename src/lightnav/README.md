# LightNav-derived packages

This directory contains the robot-side packages derived from the upstream
LightNav-0 `robot_deploy` workspace: `vln_client`, `vln_mpc`, and `vln_web`.

Keep upstream attribution and snapshot revisions documented when these packages
are updated.
