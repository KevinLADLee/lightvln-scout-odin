# LightVLN Scout integration

This directory contains the project-owned `lightvln_scout` package. It combines
the Scout safety/command adapter with the launch and configuration code that
composes the LightNav, Odin1, and Scout nodes.
